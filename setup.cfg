[tool:pytest]
DJANGO_SETTINGS_MODULE = config.settings.development

[coverage:run]
source = backend
omit =
    */migrations/*
    */tests/*
    */settings/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    if TYPE_CHECKING:
